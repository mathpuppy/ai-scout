# 复制为 .env 后按需填写（.env 已被 gitignore，不会入库；本模板可入库）。
# 全部可选：留空或删除即用 pi 本机默认配置（~/.pi/agent/settings.json + auth.json）。
# 优先级：命令行环境变量 > 本文件 > pi 本机默认（如 `PI_MODEL=xxx ./run-weekly.sh` 临时覆盖）。

# ---- LLM 配置（映射为 pi 的 --provider/--model/--api-key/--thinking）----
PI_PROVIDER=zai-coding-cn
PI_MODEL=glm-5.3:high
#PI_API_KEY=
#PI_THINKING=high

# ---- 认证类 ----
# folo 认证（或在新机器上跑一次 npx --yes folocli@latest login 存登录态）。
# 注意：此变量优先于 folocli 存储登录态——本机已 login 过就留空，填错值会覆盖真登录态。
#FOLO_TOKEN=
# gh 认证（或 gh auth login；不配则 L3 潮流线退匿名限流）
#GH_TOKEN=
